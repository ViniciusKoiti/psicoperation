{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_comment": "tsconfig do próprio pacote: consome o preset base que ele exporta (dogfooding). Fonte é JS puro com JSDoc — checkJs valida os presets sem etapa de build.",
  "extends": "./tsconfig/base.json",
  "compilerOptions": {
    "allowJs": true,
    "checkJs": true,
    "noEmit": true,
    "types": []
  },
  "include": [
    "eslint",
    "prettier",
    "vitest",
    "types",
    "tests",
    "eslint.config.js",
    "vitest.config.js"
  ]
}
